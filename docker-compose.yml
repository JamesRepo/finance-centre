services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: finance_centre
      POSTGRES_PASSWORD: finance_centre
      POSTGRES_DB: finance_centre
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
